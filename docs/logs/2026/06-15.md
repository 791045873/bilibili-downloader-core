# Development Log - 2026-06-15

### 2026-06-15

- 启动 `下载目录指定与查看`：创建 `docs/requirements/2026-06-15-download-directory-view.md`、`docs/plans/2026-06-15-download-directory-view-plan.md` 和 `docs/testing/2026/06-15-download-directory-view-testing.md`。
- 通过独立 plan audit 后实现下载目录查看切片：新增 `GET /api/download/config`，返回服务端下载根目录和配置来源。
- 更新设置页：展示服务端下载根目录、来源标签和复制操作，复制失败时保留页面主要功能并提示手动复制。
- 更新视频详情页入队弹框：明确填写的是下载根目录下的相对子目录。
- 更新下载列表：对有 `outputFile` 的任务展示实际输出文件路径。
- 更新 `docs/design/app-overview.md`，记录当前支持的下载目录行为和新增配置查询 API。
- 验证：`pnpm typecheck` 通过；`GET /api/download/config` 返回默认下载目录 `packages/server/downloads` 和 `source: default`。

### 字幕下载功能实现 (same day)

- Phase 1 (Core): `DownloadRequest.subtitleLanguages` & `DownloadExecutionRequest.subtitleLanguages` 字段变更 `downloadSubtitle?: boolean` → `subtitleLanguages?: "none" | "all" | string[]`
  - `DownloadExecutionUseCase.execute()` 中字幕筛选逻辑实现：`"none"` 跳过，`"all"` 全部写入，`string[]` 只写匹配 langKey 的条目
- Phase 2 (Adapter): `BilibiliSubtitleProvider` 导入并导出到 `createBilibiliApiAdapter` 工厂函数
- Phase 3 (Server):
  - `DownloadDto.subtitleLang?: string`
  - `TaskRecord.subtitleLang?: string`，schema 增加 `subtitle_lang TEXT` 列
  - `ALTER TABLE` 迁移策略（try/catch 处理已有数据库）
  - `BilibiliSubtitleProvider` 注入到 `executionDeps`
  - `toSubtitleLanguages()` 转换函数：`"none"→"none"`, `"zh"→["zh-CN"]`, `"en"→["en-US"]`, `"all"→"all"`, null→"none"`
- Phase 4: VideoDetail.vue TreeTable 在"编码"列之后增加"字幕"列（不下载/中文/英文/全部字幕），`doAddToQueue()` 透传 `subtitleLang` 到 API
- 独立实现审计: 34 项检查 33 项通过（1 项非代码文档已修复）
- `pnpm typecheck` 6 包全部通过，`pnpm build` 6 包全部通过
- `known-good-baselines.md` 更新为本次全量基线
