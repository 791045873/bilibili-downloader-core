# 2026-08-21

## LLM 并发调用限制（最多 2 个同时执行）

关联计划：`docs/plans/2026-08-21-llm-concurrency-limit-plan.md`；testing：`docs/testing/2026/08-21-llm-concurrency-limit-testing.md`。

### 变更

- `packages/adapters/src/llm/qwen-client.ts`：新增模块级 `AsyncLimiter`（Promise 信号量，默认上限 2，env `MAX_CONCURRENT_LLM_CALLS`），`multimodalChat` 入口包裹整个重试循环，最多 2 个 LLM 调用在途、超出排队等待。因闭包丢失属性收窄，`visionProxyUrl` 提取为局部常量。
- `packages/vision-proxy/qwen_vision_proxy.py`：`MAX_CONCURRENCY` 默认 `8` → `2`（兜底）；`do_GET` 的 `/healthz` 不再占用信号量槽位（Fix，避免并发满载时健康检查被 503 误判）。
- `packages/docker/docker-compose.yml`：server 服务新增 `MAX_CONCURRENT_LLM_CALLS:-2`；vision-proxy `QWEN_VISION_PROXY_MAX_CONCURRENCY:-2`。
- `packages/docker/.env.example` 与 `docs/architecture/2026-07-06-video-analysis-baseline.md`：并发默认值同步为 2，新增 `MAX_CONCURRENT_LLM_CALLS` 说明。

### 验证

- `pnpm typecheck` exit 0；`pnpm build` exit 0；Python `ast.parse` OK。
- 运行级 stub 冒烟：`{"peakInFlight":2,"totalReqs":4,"ok":4,"failed":0,"elapsedMs":720}`——并发钉死 2、第 3/4 排队且全部成功、无 503。
- 代理启动冒烟：`GET /healthz` 200 `{"status":"ok"}`；grep 确认 `_acquire_slot` 仅 `do_POST` 使用。
- `docker compose config --quiet` exit 0。

### 备注

- 排队语义在 Node 调用侧（编排归 Node，代理保持薄代理无业务语义）；Python 侧保留信号量作为兜底（默认 2）。部署保护区默认值变更由用户显式批准。

## 任务状态筛选支持多选（下载队列 + AI 总结）

关联计划：`docs/plans/2026-08-21-task-status-multiselect-filter-plan.md`；testing：`docs/testing/2026/08-21-task-status-multiselect-filter-testing.md`。

### 变更

- 后端 `/tasks` 与 `/summary-tasks`：`statusGroup`/`status` 查询参数改为支持逗号分隔多值（`active,failed` / `analyzing,failed`）；DB 过滤改为 `IN (...)` 查询，`active` 展开为 `created+downloading`，`Set` 去重，空串/`all`/缺省 = 不过滤，非法值仍 400；单值旧调用向后兼容。
  - `download.controller.ts` / `analysis-task.controller.ts`：`parseTaskStatusGroup` / `parseAiSummaryStatus` 改为返回数组。
  - `download.service.ts` / `analysis-trigger.service.ts`：参数类型改数组。
  - `database.service.ts`：`buildTaskStatusFilter` / `buildAiSummaryTaskFilter` 数组化，`AiSummaryTaskListFilter.status` 改为 `string[]`。
- 前端：`getTasks`/`getAiSummaryTasks` 的 `statusGroup`/`status` 改为数组并用 `URLSearchParams` 逗号拼接；`AiSummaryTasks.tsx` 与 `Downloading.tsx` 状态 `Select` 改 `mode="multiple"`，移除"全部/全部任务"项（空选=全部），placeholder 提示，`maxTagCount="responsive"`。

### 验证

- `pnpm typecheck` exit 0；`pnpm build` exit 0。
- 运行级接口验证（构建产物 + 临时 OUTPUT_DIR + better-sqlite3 注入 5 下载状态/4 总结状态）：多值、单值、all、空串、重复去重、非法 400 共 18 项 PASS（含 4 项 400）。

## 下载队列 / AI 总结页面放宽至全屏宽度

- `packages/frontend/src/App.tsx`：新增 `FULL_WIDTH_PATHS`（`/downloading`、`/summary-tasks`），`<main>` 对这两个路由不再应用 `max-w-5xl mx-auto`，列表/表格占满整屏宽度；其余页面保持原宽度不变。
- 验证：`pnpm typecheck` exit 0；frontend build exit 0。
