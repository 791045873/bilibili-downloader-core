# Development Log - 2026-08-10

### 2026-08-10

- 分析下载输出文件命名问题：`download.service.ts` 直接用标题拼文件名，同目录同名冲突时 `DownloadExecutionUseCase` 会"文件已存在即跳过"，造成同标题第二个视频假成功且落盘为第一个视频；`DownloadRequest.skipExisting` / `fileNameTemplate`、`DownloadPlan.outputFileName` 已定义但未使用。
- 与用户确认方案：P0 采用策略 A2（`{title}-{bvid}-{cid}-q{quality}.mp4`，与 `executeLowResDownload` 命名对齐）；标题清洗与长度截断守卫不单列（数据源标题为已正常使用的合法字符串）；P2 落地 `fileNameTemplate`，`{title}` 用前端展示标题、`outputPath` 目录语义不变。
- 创建讨论存档 `docs/discussions/2026-08-10-download-file-naming.md`，记录决策、根因、P2 标题来源分析及遗留开放问题（模板配置入口，推荐设置页默认 + 入队弹框覆盖）。
- 创建 P0 微计划 `docs/plans/2026-08-10-download-file-name-uniqueness-plan.md`（`Audit: skipped under micro-plan exception`，单文件约 5 行改动）。
- 创建 P2 全计划 `docs/plans/2026-08-10-download-file-name-template-plan.md`（含 DB 迁移与 API 契约变更，Phase 1 为待人类确认的产品决策，审计状态 pending）。
- 创建对应测试方向文档：`docs/testing/2026/08-10-download-file-name-uniqueness-testing.md`、`docs/testing/2026/08-10-download-file-name-template-testing.md`。
- 确认跳检语义：`DownloadExecutionUseCase` 的"已存在即跳过"基于 `NodeFileStore.exists`（`fs.access` 真实磁盘检查），DB 记录不参与；DB 有记录但文件缺失时任务会被重新下载。
- 用户决策：移除 UI 层下载去重拦截，以服务端为准 —— 前端保留基于 DB 记录的"已下载"展示标记，但用户可随时勾选并加入任意视频；`POST /api/tasks/check` 接口保留（"已下载"标记仍依赖它）；服务端不改。
- 更新讨论存档决策 4 与 Open Questions；创建 `docs/plans/2026-08-10-download-enqueue-dedup-ux-plan.md`（微计划）与 `docs/testing/2026/08-10-download-enqueue-dedup-ux-testing.md`。
- P2 Phase 1 决策确认：`fileNameTemplate` 仅设置页全局默认（选项 A），模板创建时捕获并持久化到 `task` 表，执行时读取；不做入队弹框覆盖。同步更新 P2 计划与模板测试方向（TD-P2-5/6/7）。

## 实施（P0 / UI 去重 / P2）

### P0 下载输出文件名唯一化（`docs/plans/2026-08-10-download-file-name-uniqueness-plan.md`，微计划已关闭）

- `download.service.ts` `executeTask` 文件名改为 `{title}-{bvid}-{cid}-q{videoStream.quality}.mp4`，日志补 quality 字段。
- `pnpm typecheck` 通过；逻辑脚本验证同标题不同视频/不同画质均生成不同文件名。
- 冷回放自查关闭计划；运行级真实下载留用户手动验证。

### UI 下载去重控制移除（`docs/plans/2026-08-10-download-enqueue-dedup-ux-plan.md`，微计划已关闭）

- `VideoDetail.vue` 与 `ParseResultList.vue`：`enqueued`（拦截）改为 `downloaded`（展示性"已下载"标记）；移除 24h success 门控、复选框禁用、选择守卫、加入过滤；`selectedCount` 同步；一键 AI 总结按钮仅字段改名、行为不变。
- 前端 typecheck 通过；grep 确认无 `enqueued`/`已入队` 残留。
- 冷回放自查关闭计划；运行级 UI 交互留用户手动验证。

### P2 文件名模板与命名收敛（`docs/plans/2026-08-10-download-file-name-template-plan.md`，全计划已关闭）

- 新建 `packages/server/src/download/file-naming.ts`：`buildOutputFileName` + `sanitizeFileName`，占位符 `{title}` `{bvid}` `{cid}` `{quality}` `{codec}`，未知占位符保留字面量，空模板回退默认 `{title}-{bvid}-{cid}-q{quality}`。
- `DownloadDto` 新增 `fileNameTemplate`；`task` 表新增 `fileNameTemplate TEXT`（幂等 ALTER）；`insertTask`/select 联动；`createTask` 落库、`executeTask` 读取渲染；`executeLowResDownload` 改用命名模块（恒默认，行为不变）。
- 前端：settings store `defaultFileNameTemplate`（localStorage）、`Settings.vue` 输入项、`createDownload` 透传、两个入队视图透传。
- 实施前冷回放审计通过；`{page}` 占位符因任务数据无法解析从占位符集移除，已记录偏差。
- `pnpm typecheck`、`pnpm build` 通过；编译产物 5 组命名用例 ALL PASS；内存 SQLite 冒烟（幂等 ALTER + insert/select）通过。
- 冷回放 closure audit 关闭计划；`docs/design/app-overview.md` 已更新文件名可配置说明。
- 运行级真实下载/设置页交互留用户手动验证。

### 遗留（留给用户手动运行级确认）

- 真实下载：同标题不同视频两个文件、同视频重复入队跳过、删除文件后重新下载、按模板命名落盘。
- 前端交互："已下载"标记与重新勾选、设置页模板输入保存。运行命令：`pnpm dev:server`。
