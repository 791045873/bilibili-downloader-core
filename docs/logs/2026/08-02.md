# Development Log - 2026-08-02

### 2026-08-02

- 启动 `adapter error boundary and diagnostic logging`：创建 `docs/requirements/2026-08-02-adapter-error-boundary-and-diagnostic-logging.md`，并将 `docs/context/project-context.md` 的 active requirement / active plan 切换到本次执行切片。
- 完成 Phase 1 owner-doc 对齐：更新 `docs/architecture/module-boundaries.md`，明确 adapters 默认向上抛错、仅在隐藏失败或静默降级时允许本地低频诊断，且 server 拥有高语义日志所有权。
- 更新 `docs/architecture/system-baseline.md`，补充 adapter 错误传播、低频诊断和敏感信息裁剪的稳定规则。
- 对应 testing 文档沿用 `docs/testing/2026/08-01-adapter-error-boundary-and-diagnostic-logging-testing.md`，后续 Phase 2/3 将基于该文档补代码与验证证据。
- 完成 Phase 2 传播型 adapter 收敛：新增 `packages/adapters/src/safe-error-context.ts`，并将 `resource-parser`、`http-downloader`、`aria2-downloader`、`ffmpeg-merger`、`qwen-client` 的异常消息收敛为安全摘要上下文，避免直接暴露完整 URL、完整路径和上游响应体。
- 完成 Phase 3 隐藏失败诊断：为 `subtitle-provider`、`web-client`、`auth-provider`、`aria2-downloader`、`ffmpeg-screenshot`、`subtitle-srt-parser`、`tencent-cos-temp-image-store`、`node-file-store`、`task-store` 补充低频 `warn` 诊断，保持原有 fallback / 空结果契约不变。
- 验证：`pnpm --filter @bilibili-downloader/adapters typecheck` 通过；`pnpm typecheck` 通过；`pnpm build` 通过。运行环境仍存在 Node engine warning（期望 24.16.0，当前 22.22.3），但不影响验证完成。
- 手工证据：built-module 探针确认 `ResourceParseError` 对 URL 输入已做 `?...` 摘要；built-module 探针确认 `subtitle-srt-parser` 对 malformed block 输出单条 `WARN` 并继续返回其余条目。
- 用户后续明确要求忽略 `qwen-client` 上游响应体裁剪的额外手工探针验证；已将该条 testing direction 裁定为 out-of-scope proof waiver，保留现有代码审查证据，不再继续追该条运行时验证。
- closure：`docs/plans/2026-08-01-adapter-error-boundary-and-diagnostic-logging-plan.md` 已按 cold-replay closure audit 收口；testing 文档除用户明确 waived 的 runtime probe 外均已 passed 或 adjudicated out of scope。
- 启动 `server observability logging` 计划实现：新增 `packages/server/src/logging/server-log.util.ts` 与 `packages/server/src/logging/request-logging.interceptor.ts`，并在 `packages/server/src/app.module.ts` 注册全局 request interceptor；`packages/server/src/main.ts` 启动/致命错误日志改为 Nest Logger + 结构化安全上下文。
- 完成下载与任务链路 observability：`packages/server/src/download/download.controller.ts`、`packages/server/src/download/download-scheduler.ts`、`packages/server/src/download/download.service.ts`、`packages/server/src/database/database.service.ts` 补齐 invalid-id / task-not-found / queue claim / slot release / progress snapshot / status transition / analysis sub-task 摘要日志。
- 完成分析与轻量接口 observability：`packages/server/src/analysis/analysis.controller.ts`、`packages/server/src/analysis/analysis-trigger.service.ts`、`packages/server/src/analysis/analysis-engine.ts`、`packages/server/src/analysis/screenshot-source-resolver.ts`、`packages/server/src/notification/notification.service.ts`、`packages/server/src/parse/parse.service.ts`、`packages/server/src/video/video.controller.ts` 补齐 trigger/fallback/cleanup/notification/parse/video 关键分支日志，并移除了 in-scope server `console` 诊断。
- 验证期间发现并修复三个运行时缺口：`DatabaseService.deleteTask/clearTasks()` 现在先删除 `analysis_sub_task` 以避免 SQLite 外键失败；`DownloadService.restoreTaskCacheFromDatabase()` + `DownloadScheduler.onModuleInit()` 现在会在启动调度前恢复持久化任务缓存；`AnalysisTriggerService` 修正 `isNil` 运行时导入与字段判空逻辑，避免下载完成后误判“缺少分析字段”。
- 手工 proof：在 `:3001`（禁调度实例）回放 create/stop/resume/delete/clear/check/get/auto-summary 等队列态成功与失败分支；在 `:3000` 回放实际下载、高低清并发、analysis trigger、empty-summary、notification skip、temporary cleanup、parse/video 成功与失败分支。Route matrix 已收口到 20 个非 protected 端点，3 个 auth 端点继续保持 `blocked-protected`。
- 验证命令：`pnpm --filter @bilibili-downloader/server typecheck` 多次通过；最终 `pnpm typecheck` 与 `pnpm build` 通过。运行环境仍存在 Node engine warning（期望 24.16.0，当前 22.22.3），但不阻塞本次 proof。
- 用户显式接手环境依赖型判断：`/api/analysis/run` 真实成功路径、SMTP-connected notification outcome、screenshot fallback permutation、以及 `/space/.../upload/video` parse 支持范围不再由 AI 继续追到运行时闭环，已在 testing 文档内记为 user-owned adjudication。
- closure：`docs/plans/2026-08-01-server-observability-logging-plan.md` 以 cold-replay closure audit 收口；`docs/testing/2026/08-01-server-observability-route-matrix.md` 与 `docs/testing/2026/08-01-server-observability-logging-testing.md` 现已提供 file-backed closure evidence。