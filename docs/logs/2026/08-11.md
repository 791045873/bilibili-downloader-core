# Development Log - 2026-08-11

### 2026-08-11

- 修正 AI 总结链路中 Python 视觉代理不可用时的错误处理：`analysis-engine.ts` 中 LLM 多模态调用失败（如 Python 服务未启动、`QWEN_VISION_PROXY_URL` 不可达）不再回退生成空文档，而是向上抛错，使任务以 `failed` 状态结束并保留真实错误信息。
- 空文档路径仅保留给"LLM 正常返回但无有效总结段落"的场景，与需求 `2026-07-06-video-analysis-summary.md`（LLM 调用失败的重试/降级策略暂不涉及）对齐；Python 薄代理为 AI 总结必选依赖。
- `pnpm typecheck` 通过。

## AI 总结全路径分析与修复计划

- 创建全路径分析文档 `docs/analysis/2026-08-11-ai-summary-fullpath-analysis.md`：梳理所有用户入口（一键总结/任务页重新总结/自动触发）、三套并行状态模型、磁盘 vs DB 一致性缺口、并给出"不需重写架构，但需补资产与状态对账层"的结论。
- 按分析结果创建三份修复计划（均通过独立 subagent plan 审计，task `ses_010896279ffelAsyfTE2tfH5fx`）：
  1. `docs/plans/2026-08-11-ai-summary-resource-availability-plan.md`（P0 资源可用性对账：复用前磁盘校验 + 缺失重下 + 截图源降级链恢复，含注入 resolver 的审计补充项）。
  2. `docs/plans/2026-08-11-ai-summary-trigger-robustness-plan.md`（P0 触发健壮性：trigger 全量错误落地 failed + 守卫统一 + 认领原子化 + 启动对账恢复）。
  3. `docs/plans/2026-08-11-ai-summary-state-architecture-consolidation-plan.md`（P1 架构收敛：AnalysisVideoResolver 资产层 + 状态单一来源 + 子任务资源级键 + summaryDir 稳定化）。
- 配套创建三份 testing 方向文档（`docs/testing/2026/08-11-*.md`）。
- 审计结论：三份计划 baseline 与 live 代码全部实质一致，行号漂移与补充事实已并入计划；计划处于 `planned` 状态，待实施。

## 实施（P0 ×2 + P1 ×1，按计划顺序）

### P0 资源可用性对账（`docs/plans/2026-08-11-ai-summary-resource-availability-plan.md`，已关闭）

- `DownloadService.fileExists()` 暴露磁盘校验；LLM 分析视频选择收敛到新建 `AnalysisVideoResolver.resolveAnalysisVideo`（低清→高清→缺失重下）；`screenshotVideoPath` 仅透传真实存在文件并为触发路径注入 resolver（恢复远端流降级链）；截图源回退与 `resolveTaskForAnalysis` 重载均前置磁盘校验。
- `pnpm typecheck`、`pnpm build` 通过；DB 冒烟 5/5。运行级删除文件重触发留用户手动。

### P0 触发健壮性（`docs/plans/2026-08-11-ai-summary-trigger-robustness-plan.md`，已关闭）

- `trigger()` 拆分为守卫 + 原子认领（`claimAiSummaryTask`）+ 低清等待 + `runAnalysis()`（全部前置决策与分析在 try/catch，异常统一落 failed + 通知）；资源级入口 `POST /api/analysis/trigger` 增加进行中拦截；`onLowResFinished` 成功直接续跑 `runAnalysis`、失败落 failed 并通知。
- 启动对账 `reconcileStaleAnalysisState()`：遗留 created 子任务与 pending/analyzing 总结标 failed，消除重启永久等待；保守不自动 resume。
- `pnpm typecheck`、`pnpm build` 通过；DB 冒烟（认领互斥 5/5 + 对账）通过。

### P1 状态与资产架构收敛（`docs/plans/2026-08-11-ai-summary-state-architecture-consolidation-plan.md`，已关闭）

- 新建 `analysis-video-resolver.ts`（资产决策层，含 `resolveAnalysisVideo` + 截图源 `resolve`），`DefaultScreenshotSourceResolver` 并入并删除原文件；`AnalysisController` / `AnalysisTriggerService` 均改注入新 resolver。
- 状态单一来源：`updateTaskStatus` 移除 summary 双写；`taskSelectSql` 直接 JOIN `ai_summary_task`；`initSchema` 幂等迁移历史 `task.summary_status` → `ai_summary_task`。
- `analysis_sub_task` 资源级键：部分唯一索引 `(bvid,cid,quality) WHERE status!='failed'` + 去重迁移 + `getAnalysisSubTasks(bvid,cid)` 资源查询 + 调度按资源去重；`resolveSummaryDir` 改为 `summary/{bvid}-{cid}`。
- 实现偏差已记录：resolver 输入扩展（taskId/title/llmVideoDir）、部分唯一索引替代全量 UNIQUE、删除 `getAnalysisSubTasksByTaskId`。
- `pnpm typecheck`、`pnpm build` 通过；DB 迁移冒烟 6/6。运行级（真实 DB 迁移、真实下载回归、UI 展示）留用户手动。

### 独立闭核算（explore subagent，task `ses_010607650ffePUP1xWYzjMDbt1`）

- 三份计划闭核算结论：资源可用性 P0=PASS、触发健壮性 P0=PASS、架构收敛 P1=PASS-with-notes。全部执行项与退出标准在真实代码落地，无"仅文档声称"项、无功能性回归。
- 必须修正项已处理：P1 计划 Phase 1 Status 标签 `planned` → `completed`。
- 采纳审计建议：移除 `shouldReuseDownloadedVideo`（决策已由 resolver 接管，其残留的 `parseVideo` 网络调用可能成为分析失败点），以 `Analysis video source resolved` 日志保留可观测性；测试文档补充运行级场景留用户手动的原因；P1 计划补记接口签名偏差。
- 遗留：冒烟脚本为一次性脚本未入库；后续同类迁移建议 subagent 复核。

### summary 目录命名调整（闭核算后）

- 按用户要求将 summary 输出目录从 `{bvid}-{cid}/` 调整为 `{标题}-{bvid}-{cid}/`（标题在前、完整不截断，非法字符清洗；bvid-cid 为稳定锚点）。
- 同资源已存在 summary 目录时复用（`readdirSync` 探测 `-{bvid}-{cid}` 后缀或精确候选名），标题变化不产生新孤儿目录。
- `pnpm typecheck`、`pnpm build` 通过；架构基线文档已同步更新。

### AI 总结任务列表表格化（`docs/plans/2026-08-11-ai-summary-tasks-table-plan.md`，已关闭）

- 服务端：`ai_summary_task` 新增 `key_timestamps TEXT` 列（幂等迁移）；`AnalysisOutput` 新增 `keyNodes`（段落标题+时间戳）；`upsert` 支持写入/保留、`claim` 认领即清空（重跑 stale 语义：认领即清、成功才写）；`getAiSummaryTasks` 解析返回数组（坏 JSON 回退 `[]`）。
- 前端：`AiSummaryTasks.vue` 卡片式改为表格（视频标题/状态/总结时间/关键时间节点/总结输出/更新时间），`formatTime` 本地化，空态 `—`。
- 独立 plan 审计（task `ses_01012609affeVoGZUK5APe3hiW`）发现重跑 stale 语义漏洞，已定案并写入计划/测试文档。
- `pnpm typecheck`、`pnpm build` 通过；DB 冒烟 9/9。运行级（真实 B 站视频 completed 列表）留用户手动。

### 回退与修正：关键时间节点 → 执行耗时（用户澄清）

- 用户澄清「重要的时间节点」指 AI 总结任务的**执行耗时节点**（LLM 分析耗时、截图耗时、总计），而非视频内容时间戳。
- 已回退视频时间戳方案：移除 `key_timestamps` 列 / `AnalysisOutput.keyNodes` / 前端关键时间节点列，grep `keyTimestamps|key_timestamps|keyNodes` 零残留。
- 改为 `execution_timing`：`AnalysisEngine.analyze()` 测量 `llmMs`/`screenshotMs`/`totalMs`，`AnalysisOutput.timing` 返回，成功分支持久化到 `ai_summary_task.execution_timing`，认领即清空（stale 语义沿用），`getAiSummaryTasks` 解析返回对象。
- 前端表格新增「执行耗时」列（`LLM 12.0s · 截图 5.0s · 总计 18.0s`，<1s 显示 ms），空态 `—`。
- `pnpm typecheck`、`pnpm build` 通过；DB 冒烟 7/7。计划与测试文档已按执行耗时语义重写。
- 执行耗时回退逻辑：`executionTiming` 为空（历史任务/空内容总结）时，前端用 `lastCompletedAt - lastTriggeredAt` 推导「总计」展示；有明细时仍显示完整明细。`pnpm typecheck`/`pnpm build` 通过。

### 遗留（留给用户手动运行级确认）

- 删除低清/高清文件后重新触发 AI 总结的自动恢复（真实 B 站视频）。
- 服务重启后对账与重触发恢复。
- 异常注入（缺 QWEN 配置）触发时任务落 failed 且 UI 可见。
- 并发双点触发只认领一次。
- 真实 DB 迁移后任务列表 / AI 总结任务页展示一致。

### Vision Proxy Python 依赖与代码最佳实践改进（`docs/plans/2026-08-11-vision-proxy-python-best-practice.md`，已关闭）

- 依赖管理：`requirements.txt` 迁移为 `packages/server/python/pyproject.toml`（setuptools 空包、锁定 `dashscope==1.26.6`、`python-dotenv==1.2.2`）；`setup-vision-proxy` 改为在 `packages/server/python/.venv` 中 `pip install .`，幂等检查基于 venv 内依赖导入（postinstall 静默跳过），不再污染全局 Python。
- 启动链路：新增 `scripts/start-vision-proxy.mjs`（优先 venv Python，缺失回退系统 Python）；`packages/server/package.json` 的 `start:vision-proxy` 改为 `node ../../scripts/start-vision-proxy.mjs`（pnpm 以包目录为 CWD）。
- 日志：`qwen_vision_proxy.py` 引入 `logging`（启动横幅、请求行、dashscope 失败、异常 traceback 均输出 stderr），`missing_dependency_exit` 提示更新为指向 pyproject + `pnpm setup:vision-proxy`。
- 文档同步：README、`docs/architecture/2026-07-06-video-analysis-baseline.md`、`docs/context/codebase-map.md`、`.gitignore`（补 `*.egg-info/`、`build/`）。
- 独立 plan 审计（REVISE 后复审 APPROVED-WITH-NOTES，task `ses_00ec532a1ffe...` / `ses_00ec15753f...`）修正：start 相对路径、缺依赖提示、egg-info、typecheck 断言；独立闭核算（explore subagent，task `ses_00eb861e9ff...`）结论 PASS-WITH-NOTES。
- 验证：setup 首次建 venv 安装成功 / 二次幂等跳过 / `--postinstall` 静默 exit 0；`pnpm --filter @bilibili-downloader/server start:vision-proxy` 真实启动并监听 8765，stderr 输出请求日志与 traceback；`pnpm --filter @bilibili-downloader/server typecheck` 通过；`.venv/`、`*.egg-info/`、`build/`、`__pycache__/` 均被 gitignore 忽略。

### 任务与 AI 总结记录删除操作（`docs/plans/2026-08-11-task-and-summary-delete-plan.md`，已关闭）

- 需求：`docs/requirements/2026-08-11-task-and-summary-delete.md`。删除语义（经用户确认）：仅删数据库记录、不动磁盘、两条路径独立；AI 总结记录进行中（pending/analyzing）禁止删除。
- 服务端：新增 `DELETE /api/summary-tasks/:id`（非法 id 400 / 不存在 404 / 进行中 409 / 成功 200 `{message:"已删除"}`），DB 层新增 `getAiSummaryTaskById` + 条件 `deleteAiSummaryTask`（`WHERE id=? AND status NOT IN ('pending','analyzing')`，关闭 check-then-delete 竞态），service 层包装转发；下载任务删除链路未改动。
- 前端：`AiSummaryTasks.vue` 新增操作列删除按钮（进行中行禁用显示"进行中"）；`Downloading.vue` 非 downloading 态新增删除按钮（复用 handleDelete），downloading 保留"取消"；`api/index.ts` 新增 `deleteAiSummaryTask`。
- 受保护区证据：owner doc（app-overview.md）补充两条 DELETE 集成点与删除语义；testing 文档记录冒烟证据；冒烟 13/13 PASS（completed 删除后磁盘文件保留、pending 409、404/400、任务删除不联动删总结）。
- 独立 plan 审计（REVISE→复审 APPROVED-WITH-NOTES）与闭核算（PASS-WITH-NOTES，采纳条件删除建议）；`pnpm typecheck`、`pnpm build` 通过。
