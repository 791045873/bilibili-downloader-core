# docker compose 环境变量示例。
# 将需要的变量写入本目录下的 .env（已被根 .gitignore 的 `.env` 规则忽略，不会提交），
# 或直接在当前 shell 环境导出后运行 `pnpm docker:run`。

# 镜像版本（一般无需手工配置）：`compose.mjs` 每次运行都会从对应包文件推导并写入
# 本目录 .env——SERVER_VERSION 取 packages/server/package.json 的 version，
# VISION_PROXY_VERSION 取 packages/vision-proxy/pyproject.toml 的 version，
# 镜像 tag 即对应包版本。如需临时覆盖（例如发测试 tag），设置同名环境变量即可：
#   SERVER_VERSION=9.9.9-test pnpm docker:build
# 注意：覆盖值会同步写入本目录 .env 并持续生效，直到下次不带该环境变量运行
# pnpm docker:* 时回写为包版本。
# SERVER_VERSION=0.0.1
# VISION_PROXY_VERSION=0.0.1

# 视觉代理密钥：由前端设置页配置进数据库（llm.apiKey），Node 经 Authorization 头传给
# vision-proxy 容器，不再需要任何 DashScope 密钥环境变量；SDK 基址在代理代码中写死。

# Node 访问 Python 视觉代理的完整 URL，可完全自定义，指向任意可访问的代理地址。
# 默认仅指 compose 网络内服务名（vision-proxy:8765）；如代理部署在其他主机/端口，
# 改这里即可（例如 http://192.168.1.10:8765/v1/chat/completions）。
# QWEN_VISION_PROXY_URL=http://vision-proxy:8765/v1/chat/completions

# 可选：代理健壮性参数（均有默认值，可不配置）。
# QWEN_VISION_PROXY_MAX_BODY_BYTES：代理 HTTP body 上限（默认 16777216=16MB），超出返回 413。
# QWEN_VISION_PROXY_MAX_CONCURRENCY：代理兜底并发请求上限（默认 2），超出返回 503。
# QWEN_VISION_PROXY_SOCKET_TIMEOUT：连接 socket 读/写超时秒数（默认 120）。
# QWEN_VISION_PROXY_TIMEOUT_MS：Node 侧多模态代理调用超时毫秒数（默认 600000=10 分钟）。
# MAX_CONCURRENT_LLM_CALLS：Node 侧同时最多执行的大模型调用数（默认 2），超出排队等待。

# 可选：日志保留天数（默认 7）。
# LOG_MAX_FILES=7

# 可选：下载并发上限（默认 10）。
# MAX_CONCURRENT_DOWNLOADS=10

# 可选：下载/日志宿主机目录（默认解析到 ${HOME:-$USERPROFILE}/Downloads/bilibili_download，
# 两个容器共享该目录）。Windows 宿主 HOME 为空时自动回退 USERPROFILE。
# DOWNLOAD_HOST_PATH=C:\path\to\downloads

# 必填：云端 PostgreSQL 连接串（本需求起 server 改为直连云数据库，不再使用本地 SQLite）。
# 示例：postgres://user:password@pgm-xxx.pg.rds.aliyuncs.com:5432/dbname
# 密码含特殊字符需 URL 编码：@ -> %40，: -> %3A，/ -> %2F，# -> %23。
# 需保证运行 server 的主机/容器出口 IP 在 RDS 白名单内。未配置时 compose 会报错要求设置。
# DATABASE_URL=postgres://user:password@pgm-bp1zn6syt3qkqy1nfo.pg.rds.aliyuncs.com:5432/ai_summary

# 知识发布到腾讯云 COS 所需配置（可选；不配置则知识发布跳过并标记 failed）。
# 需在腾讯云 COS 控制台创建存储桶（BucketName-APPID 格式）并开通"公有读私有写"，
# 否则公网 URL 匿名读取会返回 403，md 预览图片无法加载。
# TENCENT_COS_SECRET_ID=
# TENCENT_COS_SECRET_KEY=
# TENCENT_COS_REGION=ap-chengdu
# TENCENT_COS_BUCKET=ai-summary-1325700411
# 可选：自定义公网 URL 前缀（默认 https://<bucket>.cos.<region>.myqcloud.com）
# TENCENT_COS_PUBLIC_URL_PREFIX=https://your-custom-domain.com

# 注意：vision-proxy 监听 0.0.0.0:8765 仅为 compose 内部网络可达（Node 经服务名
# vision-proxy 访问），请勿把 8765 映射到宿主机端口。
# 数据库 schema 由 Prisma 管理（P3/P4 起）：server 容器启动自动执行 prisma db init（幂等），无需额外配置
# 向量化（Phase 2）：embedding 走 DashScope OpenAI 兼容端点；API Key 复用设置页 llm.apiKey，
# 以下三项有默认值，可按需覆盖（EMBEDDING_DIMENSIONS 当前仅支持 1024，变更需迁移+存量重算）：
# EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# EMBEDDING_MODEL=qwen3.7-text-embedding
# EMBEDDING_DIMENSIONS=1024
